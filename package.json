{
  "name": "b-splines",
  "version": "1.0.0",
  "scripts": {
    "build": "babel src --presets es2015,stage-0 -d es2015/src && babel examples --presets es2015,stage-0 -d es2015/examples && npm run copy",
    "copy": "copyfiles --up 1 examples/*.html examples/*.css examples/*.svg es2015/examples"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "copyfiles": "latest"
  }
}
